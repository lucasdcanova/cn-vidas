import React from 'react';
import { useLocation } from 'wouter';

// Versão simplificada que não usa múltiplos hooks complexos
// Card removido conforme solicitado - médicos podem acessar emergências através da seção de próximas consultas
export const EmergencyBannerSimple = ({ doctorProfile }: { doctorProfile: any }) => {
  // Retornar null para remover o componente da interface
  return null;
};

export default EmergencyBannerSimple;